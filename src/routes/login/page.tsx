import AuthDiscordCard from '@/components/auth-providers/discord-card';

const LoginPage = () => {
  return (
    <main>
      <div className="grid">
        <AuthDiscordCard />
      </div>
    </main>
  );
};

export default LoginPage;
